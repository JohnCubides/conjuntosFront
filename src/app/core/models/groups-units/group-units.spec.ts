import { GroupUnitsIndependent } from './group-units';

describe('Group', () => {
  const Group: GroupUnitsIndependent = {
    id: 1,
    independentAmount: 1,
    name: 'test',
    estateUnit: 1
  };
  it('should create an instance', () => {
    //expect(new User()).toBeTruthy();
  });
});
