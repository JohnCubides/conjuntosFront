import { EventsForms } from './events-forms';
import { Renderer2, Injector } from '@angular/core';

describe('EventsForms', () => {
  it('should create an instance', () => {
    // expect(new EventsForms(new Injector()).toBeTruthy();
  });
});
