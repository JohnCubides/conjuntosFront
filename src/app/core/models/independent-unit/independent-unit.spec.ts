import { Independentunit } from './independent-unit';

describe('Group', () => {
  const Group: Independentunit = {
    id: 1,
    Name: 'jeyson',
    Cadastre: 1,
    SquareMeter: 200,
    Status: 1,
    GroupId: 1
  };
  it('should create an instance', () => {
    //expect(new User()).toBeTruthy();
  });
});
