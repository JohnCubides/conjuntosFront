/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { AdminIndependentUnitsComponent } from './admin-independent-units.component';
import { RouterTestingModule } from '@angular/router/testing';
import { ApiService } from 'src/app/core/https/http.service';
import { HttpClientTestingModule } from '@angular/common/http/testing';
import { StoreModule } from '@ngrx/store';

describe('AdminIndependentUnitsComponent', () => {
  let component: AdminIndependentUnitsComponent;
  let fixture: ComponentFixture<AdminIndependentUnitsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [AdminIndependentUnitsComponent],
      imports: [
        RouterTestingModule,
        HttpClientTestingModule,
        StoreModule.forRoot({})
      ],
      providers: [ApiService]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AdminIndependentUnitsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/